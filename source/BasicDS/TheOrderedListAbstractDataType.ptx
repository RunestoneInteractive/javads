<?xml version="1.0"?>
<section xml:id="basic-ds_the-ordered-list-abstract-data-type" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>The Ordered List Abstract Data Type</title>
  <p>We will now consider a type of list known as an <em>ordered list</em>. For example, if the list of integers shown in the previous section were an ordered list (ascending order), then it could be written as 17, 26, 31, 54, 77, and 93. Since 17 is the smallest item, it occupies the first position in the list. Likewise, since 93 is the largest, it occupies the last position.</p>
  <p>The structure of an ordered list is a collection of items where each
    item holds a relative position that is based upon some underlying
    characteristic of the item. The ordering is typically either ascending
    or descending and we assume that list items have a meaningful comparison
    operation that is already defined. Many of the ordered list operations
    are the same as those of the unordered list, but there are also some differences since we don't have the freedom to put items in arbitrary locations.</p>
  <p>
    <ul>
      <li>
        <p><c>add(item)</c> adds a new item to the list making sure that the order is preserved. It needs the item and returns nothing.</p>
      </li>
      <li>
        <p><c>remove(item)</c> removes the item from the list. It needs the item and modifies the list. It will throw an exception if the item is not present in the list.</p>
      </li>
      <li>
        <p><c>indexOf(item)</c> searches for the item in the list and returns the first index where it is found. It needs the item and returns the index of the item, or a -1 if not found.</p>
      </li>
      <li>
        <p><c>get(index)</c> returns the item in the list at position <c>index</c>. The index must be at least 0 and no more than one less than the length of the list; otherwise, an exception is thrown.</p>
      </li>
      <li>
        <p><c>isEmpty()</c> tests to see whether the list is empty. It needs no parameters and returns a boolean value.</p>
      </li>
      <li>
        <p><c>size()</c> returns the number of items in the list. It needs no parameters and returns an integer.</p>
      </li>
    </ul>
  </p>


  <p>Here is an interface representing an ordered list:</p>

  <listing xml:id="lst-orderedlist-interface">
    <title>Interface representing Ordered List ADT</title>
    <program xml:id="prog-orderedlist-interface" label="prog-orderedlist-interface" language="kotlin">
      <code>
<xi:include parse="text" href="samplecode/src/main/kotlin/OrderedListADT.kt"/>
      </code>
    </program>
  </listing>

</section>
